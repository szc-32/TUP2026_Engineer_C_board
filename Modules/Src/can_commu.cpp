/**
 * @file can_commu.cpp
 * @brief CAN communication extension point.
 *
 * This translation unit is intentionally minimal and keeps the include linkage
 * for projects that only use declarations in can_commu.h.
 */

#include "can_commu.h"
